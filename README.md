# Git Branching Lab

Tiny Node.js API for practicing a team Git workflow.

Current endpoints:
- GET /
- GET /health

Exercise:
Add:
GET /greet?name=Mustafa

Expected:
{"message":"Hello, Mustafa"}

If name is missing:
{"message":"Hello, Guest"}

Do not implement the feature on main.
Create a feature branch first.
# git-branching-lab
